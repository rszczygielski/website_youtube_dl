from enum import Enum

class ConfigKeys(Enum):
    GLOBAL = "global"
    PLAYLISTS = "playlists"
    SWUNG_DASH = "~"
    MUSIC = "Music"
    PATH = "path"
    WRITE = "w"
    CONFIG_ERROR = "Config file is not correct"
